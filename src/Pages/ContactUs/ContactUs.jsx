import { Helmet } from "react-helmet-async";

const ContactUs = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro || Contact US</title>
            </Helmet>
        </div>
    );
};

export default ContactUs;